spring:
  profiles:
    active: dev
  jackson:
    time-zone: Asia/Seoul

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}

jwt:
  secret: ${JWT_SECRET:todolist-dev-secret-key-must-be-at-least-32-chars-long}
  expiration: 86400000
